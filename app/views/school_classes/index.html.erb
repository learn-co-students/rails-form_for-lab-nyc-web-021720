<h1>School Classes Form</h1>

<% @school_classes.each do |c| %>
    <h3><%=c.title %></h3>

    <p>
      <%= c.room_number %><br>
      <%= link_to 'View Class', school_class_path(c) %>
    </p>

    <%end%>

    <%= link_to 'New Class', new_school_class_path %>
